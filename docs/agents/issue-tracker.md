# Issue tracker: GitHub

Issues and specs for this repo live as GitHub issues (`Bitaron/agentic-coding-guide`). Use the `gh` CLI for all operations.

## Wayfinding

Used by `/wayfinder`. The active map is issue [#2 "AI Coding Session Website — content/IA spec"](https://github.com/Bitaron/agentic-coding-guide/issues/2), labelled `wayfinder:map`, with its tickets as native GitHub sub-issues labelled `wayfinder:<type>` (`research`/`prototype`/`grilling`/`task`).

See the wayfinder skill's own GitHub tracker conventions for exact commands (map/child creation, sub-issues, blocking via issue dependencies, claiming via assignee, resolving via comment + close).
